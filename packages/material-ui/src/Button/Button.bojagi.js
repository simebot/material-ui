import React from 'react';
import Button from './Button'; // your button implementation that you marked with @bojagi before

export const primary = () => <Button variant="contained" color="primary">Primary</Button>;

export const green = () => <Button variant="contained" color="secondary">Secondary</Button>;
